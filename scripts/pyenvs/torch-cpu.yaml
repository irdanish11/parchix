name: torch
channels:
  - pytorch 
dependencies:
  - python=3.8
  - pytorch cpuonly
  - torchvision cpuonly
  - pip:
    - numpy
    - pandas
    - matplotlib
    - joblib
    - easydict
    - scikit-learn
    - flask
    - flask-cors
    - requests
    - tqdm